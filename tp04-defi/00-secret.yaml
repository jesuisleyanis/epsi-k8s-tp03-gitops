# Credentials BDD dans un Secret. En prod : Sealed Secrets / Vault (jamais en clair dans Git).
apiVersion: v1
kind: Secret
metadata:
  name: wp-db-defi
type: Opaque
stringData:
  WORDPRESS_DB_HOST: mariadb          # nom de Service interne → abstrait l'IP de la VM BDD
  WORDPRESS_DB_USER: wpuser
  WORDPRESS_DB_PASSWORD: WpDbP4ss2026
  WORDPRESS_DB_NAME: wordpress
